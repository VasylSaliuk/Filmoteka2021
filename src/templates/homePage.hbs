{{#each this}}
<li class="filmlist__item">
   
    <img width='100%' id="{{id}}" src="https://image.tmdb.org/t/p/w500{{poster_path}}" alt="{{title}}">
     <h2>{{title}}</h2>||<span class="release_date">{{release_date}}</span>
    <span class="rate">{{vote_average}}</span>
</li>
{{/each}}
 {{!-- ({ title, poster_path, vote_average, id, release_date }) => {
//       return `<li class="filmlist__item">
   
//    <img id="${id}" width='100%' src="https://image.tmdb.org/t/p/w500${poster_path}" alt="${title}">
//    <h2>${title}</h2>||<span class="release_date">${release_date}</span>
//    <span class="rate">${vote_average}</span> --}}